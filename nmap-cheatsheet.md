# Nmap Cheatsheet

- Scan all ports: `nmap -p- target`  
- Service/version detection: `nmap -sV target`  
- Aggressive scan: `nmap -A target`  
- OS detection: `nmap -O target`  
- Save output: `nmap -oN scan.txt target`  
